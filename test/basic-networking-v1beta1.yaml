apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: basic-ingress-netv1b1
  namespace: default
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  backend:
    serviceName: web
    servicePort: 8080 